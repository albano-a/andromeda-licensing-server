services:
    db:
        image: postgres:16
        environment:
            POSTGRES_USER: andromeda
            POSTGRES_PASSWORD: andromeda
            POSTGRES_DB: andromeda_license
        volumes:
            - pgdata:/var/lib/postgresql/data
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U andromeda"]
            interval: 5s
            timeout: 5s
            retries: 10

    app:
        build: ./server
        env_file: .env
        ports:
            - "8000:8000"
        volumes:
            - ./src/core/security/private_key.pem:/run/secrets/private_key.pem:ro
        depends_on:
            db:
                condition: service_healthy

volumes:
    pgdata:
